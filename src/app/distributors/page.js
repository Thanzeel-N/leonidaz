import DistributorsPage from './DistributorsPage';

export const metadata = {
  title: 'PCD Franchise Network',
  description: 'Find Leonidaz Pharmaceuticals PCD Franchise partners in your district. Explore our exclusive territory franchise network across Kerala or apply to become a PCD partner.',
};

export default function Distributors() {
  return <DistributorsPage />;
}
